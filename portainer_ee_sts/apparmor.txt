#include <tunables/global>

profile portainer_ee flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  capability,
  file,
  signal (send) set=(kill,term,int,hup,cont),
  network,
  mount,

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Portainer binary and data
  /opt/portainer/** ix,
  /data/** rw,

  # Access to options.json and other addon data
  /data/** rw,

  # SSL certificates
  /ssl/** r,

  # Docker socket access - critical for Portainer
  /var/run/docker.sock rw,
  /run/docker.sock rw,
  /{,var/}run/docker.sock rw,

  # Docker runtime access
  /sys/fs/cgroup/** r,
  /proc/sys/net/ipv4/ip_forward r,

  # Shared volumes
  /share/** rw,

  # DNS resolution
  /etc/hosts r,
  /etc/resolv.conf r,
  /etc/nsswitch.conf r,
  /etc/passwd r,
  /etc/group r,

  # Device access needed for Docker operations
  /dev/null rw,
  /dev/urandom r,
  /dev/random r,
  /dev/net/tun rw,

  # Proc filesystem
  /proc/*/mountinfo r,
  /proc/*/stat r,
  /proc/*/status r,

  # Portainer specific paths
  owner /data/portainer/** rwk,
  owner /data/tls/** rwk,

  # Allow process tracing for container management
  ptrace (read,trace) peer=docker-default,
  ptrace (read,trace) peer=unconfined,
}
