name: Update Portainer EE LTS

on:
  schedule:
    # Run daily at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:
    # Allow manual triggering

permissions:
  contents: write
  pull-requests: write

jobs:
  check-update:
    runs-on: ubuntu-latest
    outputs:
      update_available: ${{ steps.check.outputs.update_available }}
      current_version: ${{ steps.check.outputs.current_version }}
      latest_version: ${{ steps.check.outputs.latest_version }}
      changelog: ${{ steps.check.outputs.changelog }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check for updates
        id: check
        run: |
          cd portainer_ee_lts
          result=$(ADDON_PATH=. VERSION_TYPE=lts CHECK_ONLY=true JSON_OUTPUT=true bash ../.github/scripts/update-portainer.sh)
          echo "Result: $result"

          update_available=$(echo "$result" | jq -r '.update_available')
          current=$(echo "$result" | jq -r '.current')
          latest=$(echo "$result" | jq -r '.latest')
          changelog=$(echo "$result" | jq -r '.changelog')

          echo "update_available=$update_available" >> $GITHUB_OUTPUT
          echo "current_version=$current" >> $GITHUB_OUTPUT
          echo "latest_version=$latest" >> $GITHUB_OUTPUT
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo "$changelog" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

  update-version:
    needs: check-update
    if: needs.check-update.outputs.update_available == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Update Portainer EE LTS
        id: update
        run: |
          cd portainer_ee_lts
          ADDON_PATH=. VERSION_TYPE=lts CHECK_ONLY=false JSON_OUTPUT=false bash ../.github/scripts/update-portainer.sh

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: |
            Update Portainer EE LTS to ${{ needs.check-update.outputs.latest_version }}

            Automated update from ${{ needs.check-update.outputs.current_version }} to ${{ needs.check-update.outputs.latest_version }}
          branch: update-portainer-lts-${{ needs.check-update.outputs.latest_version }}
          delete-branch: true
          title: "Update Portainer EE LTS to ${{ needs.check-update.outputs.latest_version }}"
          body: |
            ## Portainer EE LTS Update

            This automated PR updates Portainer EE LTS from `${{ needs.check-update.outputs.current_version }}` to `${{ needs.check-update.outputs.latest_version }}`.

            ### Changelog

            ${{ needs.check-update.outputs.changelog }}

            ### Changes

            - Updated `config.yaml` version
            - Updated `build.yaml` PORTAINER_VERSION
            - Updated `Dockerfile` PORTAINER_VERSION
            - Updated documentation files
            - Updated CHANGELOG.md

            ### Release Notes

            Full release notes: https://github.com/portainer/portainer/releases/tag/${{ needs.check-update.outputs.latest_version }}

            ---

            🤖 This PR was automatically generated by the Update Portainer LTS workflow
          labels: |
            automated
            portainer
            lts
            update
          draft: false
